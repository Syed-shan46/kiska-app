import 'package:flutter/material.dart';

class CreateAccountWid extends StatelessWidget {
  const CreateAccountWid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}